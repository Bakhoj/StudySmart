<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.anders.studysmart" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name=".MainAct"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeWorkCalendar.HWCAct"
            android:label="@string/title_activity_hwc" >
        </activity>
        <activity
            android:name=".LogIn.LogInAct"
            android:label="@string/title_activity_log_in" >
        </activity>
        <activity
            android:name=".StudyGroupManager.SGMAct"
            android:label="@string/title_activity_sgm" >
        </activity>
    </application>

</manifest>
